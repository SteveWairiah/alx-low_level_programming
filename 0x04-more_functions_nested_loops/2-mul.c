#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers
 * @x: First number to multiply
 * @y: Second number to multiply with first number
 * return: Result
 */

int mul(int x, int y)
{
	return (x * y);
}
