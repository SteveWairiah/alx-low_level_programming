#include <stdio.h>
/**
 * main - Entry point
 * Description: prints the lowercase alphabet in reverse
 * Return: Always 0
 */

int main(void)
{
	int n = 122

	for (n = 122; n > 96; n--)
	{
		putchar(n);
	}
	return (0);
}
